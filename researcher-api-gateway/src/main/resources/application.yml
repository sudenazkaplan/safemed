server:
  port: 8080

spring:
  application:
    name: researcher-api-gateway
  cloud:
    gateway:
      routes:
        - id: schema-registry-route
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/schemas/**